{
  "compilerOptions": {
    "target": "ESNext",                          // 输出 ECMAScript 版本
    "module": "ESNext",                     // 模块系统
    "types": ["vite/client"] ,
    "moduleResolution": "Node",
    "declaration": false,                      // 生成 .d.ts 文件
    "strict": true,                           // 开启严格模式
    "esModuleInterop": true,                  // 启用 ES 模块的互操作性
    "skipLibCheck": true,                     // 跳过库文件检查
    "forceConsistentCasingInFileNames": true, // 强制文件名一致性
    //  "noUnusedLocals": false,      //如果存在未使用的局部变量，编译器会发出一个错误。
    // "noUnusedParameters": false,  //如果存在未使用的函数参数，编译器会发出一个错误。
    "outDir": "./dist",  // 输出目录，可以将生成的所有文件统一放在一个文件夹中
    "sourceMap": false ,  // 禁用生成 *.js.map 文件
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"],
      "@components/*": ["src/components/*"]
    },
    "jsx": "react-jsx",  // 使用的 JSX 选项
    "resolveJsonModule": true  // 允许导入 JSON 模块
    // // 解决prettier对于装饰器语法的警告
    // "experimentalDecorators": true,
    // // 解决.jsx文件无法快速跳转的问题
    // "jsx": "preserve"
  },
  "files": [],
  "include": ["src/**/*","auto-imports.d.ts"], // 确保包含 src 文件夹中的文件
  // "include": ["src/**/*.ts", "src/**/*.d.ts", "src/**/*.tsx"],
  "exclude": [
    "dist",
    "node_modules"
  ],
  "references": [
    // { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
 
}
