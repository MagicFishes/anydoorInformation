# 📁 项目结构

## 🗂️ 目录结构

```
src/
├── router/                    # 路由配置 ⭐
│   ├── computer/              # PC 端路由模块
│   ├── mobile/                # 移动端路由模块
│   ├── generateRoutes.tsx     # 路由生成 + 守卫
│   └── types.ts               # 类型定义
│
├── layouts/                   # 布局组件
│   ├── AdminLayout.tsx        # PC 后台布局（侧边栏）
│   ├── MobileLayout.tsx       # 移动端布局（底部导航）
│   └── BasicLayout.tsx        # 基础布局
│
├── pages/                     # 页面组件
│   ├── home/                  # PC 端页面
│   ├── mobile/                # 移动端页面
│   ├── DevTools/              # 开发工具
│   │   └── RouteTest.tsx      # 路由测试工具
│   └── Login/                 # 登录页
│
├── features/                  # Redux 状态模块
│   └── appSlice/              # 应用状态（isMobile）
│
├── store/                     # 状态管理
│   └── store.ts               # Redux store
│
├── components/                # 通用组件
│   └── SidebarMenu/           # 侧边栏菜单
│
└── App.tsx                    # 应用入口 ⭐
```

---

## 📂 核心文件说明

### 路由系统

| 文件 | 功能 |
|------|------|
| `router/generateRoutes.tsx` | 动态生成路由 + 跨端守卫 |
| `router/computer/homeModule.tsx` | PC 端首页路由模块 |
| `router/mobile/index.tsx` | 移动端路由配置 |
| `App.tsx` | 监听窗口变化 + 自动导航 |

### 布局组件

| 组件 | 用途 |
|------|------|
| `AdminLayout` | PC 后台布局（左侧侧边栏） |
| `MobileLayout` | 移动端布局（底部导航） |
| `BasicLayout` | 简单页面布局（无侧边栏） |

---

## 🔧 技术栈

### 核心库

- **React 19** - UI 框架
- **TypeScript** - 类型安全
- **Vite** - 构建工具

### 路由 & 状态

- **React Router v7** - 路由管理
- **Redux Toolkit** - 状态管理
- **Redux Persist** - 状态持久化

### UI 组件

- **Ant Design** - UI 组件库
- **Tailwind CSS** - 原子化 CSS

---

## 📝 路由模块化

### PC 端路由

```
router/computer/
├── index.tsx          # 路由模块集合
├── homeModule.tsx     # 首页模块
├── hooksModule.tsx    # Hooks 示例模块
└── testModule.tsx     # 测试模块
```

### 移动端路由

```
router/mobile/
├── index.tsx          # 路由配置
├── homeModule.tsx     # 首页
├── appsModule.tsx     # 应用页
└── profileModule.tsx  # 个人中心
```

---

## 🎨 状态管理

```
features/
└── appSlice/
    └── app.ts         # isMobile 状态
```

**核心状态：**
- `isMobile: boolean` - 是否为移动端（< 768px）

---

## 📚 相关文档

- [路由守卫](./ROUTE_GUARD.md)
- [响应式系统](./RESPONSIVE.md)
